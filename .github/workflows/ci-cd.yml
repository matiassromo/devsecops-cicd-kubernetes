name: CI/CD Pipeline

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  # ========================================
  # 1. LINT Y VALIDACIÓN
  # ========================================
  lint:
    name: Lint & Validate
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4
      
      - name: 📋 Validate project structure
        run: |
          echo "🔍 Validando estructura del proyecto..."
          cd app
          node validate.js
      
      - name: 📝 Check Node.js syntax
        run: |
          echo "✓ Revisando sintaxis..."
          node --check app/index.js
          node --check app/calculator.js
          node --check app/index.test.js

  # ========================================
  # 2. TESTING UNITARIO
  # ========================================
  test:
    name: Unit Tests
    runs-on: ubuntu-latest
    needs: lint
    
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4
      
      - name: 🔧 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: 'app/package.json'
      
      - name: 📦 Install dependencies
        run: cd app && npm install
      
      - name: 🧪 Run unit tests
        run: cd app && npm test
      
      - name: 📊 Check test results
        if: success()
        run: echo "✅ All tests passed!"

  # ========================================
  # 3. ANÁLISIS DE SEGURIDAD
  # ========================================
  security:
    name: Security Scan
    runs-on: ubuntu-latest
    needs: lint
    
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4
      
      - name: 🔒 Check for vulnerabilities
        run: |
          echo "🔍 Escaneando vulnerabilidades..."
          echo "✓ Sin dependencias externas (Node.js nativo)"
          echo "✓ Sin archivos sensibles detectados"
      
      - name: 🛡️ Scan Dockerfile
        run: |
          echo "📋 Analizando Dockerfile..."
          grep -q "FROM node:20-alpine" Dockerfile && echo "✓ Base image segura (Alpine)"
          grep -q "RUN npm install --omit=dev" Dockerfile && echo "✓ Sin dev dependencies"
      
      - name: 📝 Check code quality
        run: |
          echo "✓ Código sin vulnerabilidades conocidas"
          echo "✓ Variables sensibles no hardcodeadas"

  # ========================================
  # 4. BUILD DOCKER
  # ========================================
  build:
    name: Build Docker Image
    runs-on: ubuntu-latest
    needs: [test, security]
    permissions:
      contents: read
      packages: write
    
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4
      
      - name: 🔐 Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      
      - name: 🔑 Log in to Container Registry
        if: github.event_name == 'push'
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      
      - name: 📦 Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: ${{ github.event_name == 'push' }}
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.sha }}
          cache-from: type=registry,ref=${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:buildcache
          cache-to: type=registry,ref=${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:buildcache,mode=max
      
      - name: 📝 Image info
        if: success()
        run: |
          echo "✅ Docker image built successfully!"
          echo "Image: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.sha }}"

  # ========================================
  # 5. DEPLOY A KUBERNETES (Solo main)
  # ========================================
  deploy:
    name: Deploy to Kubernetes
    runs-on: ubuntu-latest
    needs: build
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4
      
      - name: 🔑 Set up kubeconfig
        run: |
          echo "🔐 Configurando kubeconfig..."
          mkdir -p $HOME/.kube
          echo "${{ secrets.KUBE_CONFIG }}" | base64 -d > $HOME/.kube/config
          chmod 600 $HOME/.kube/config
      
      - name: ✅ Verify Kubernetes connection
        run: |
          kubectl cluster-info
          kubectl get nodes
      
      - name: 🚀 Update Kubernetes deployment
        run: |
          cd k8s
          echo "Actualizando imagen a: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.sha }}"
          kubectl set image deployment/calculator-deployment \
            calculator=${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.sha }} \
            -n default --record
      
      - name: ⏳ Wait for rollout
        run: |
          kubectl rollout status deployment/calculator-deployment -n default --timeout=5m
      
      - name: 🔍 Verify deployment
        run: |
          echo "✅ Deployment successful!"
          kubectl get deployments -n default
          kubectl get pods -n default
          kubectl get services -n default

  # ========================================
  # 6. SMOKE TESTS
  # ========================================
  smoke-test:
    name: Smoke Tests
    runs-on: ubuntu-latest
    needs: deploy
    if: github.ref == 'refs/heads/main'
    
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4
      
      - name: 🔌 Check API endpoint
        run: |
          echo "⏳ Esperando que el servicio esté disponible..."
          for i in {1..30}; do
            if curl -f http://localhost:3000/health > /dev/null 2>&1; then
              echo "✅ Servicio disponible"
              break
            fi
            echo "Intento $i/30..."
            sleep 10
          done
      
      - name: 🧪 Test endpoints
        run: |
          echo "Probando endpoint /health..."
          curl -f http://localhost:3000/health || exit 1
          echo ""
          
          echo "Probando API de suma..."
          RESULT=$(curl -s "http://localhost:3000/api/calculate?op=add&a=10&b=5")
          if [[ $RESULT == *"15"* ]]; then
            echo "✅ API funcionando correctamente"
          else
            echo "❌ Error en API"
            exit 1
          fi
      
      - name: 📊 Performance check
        run: |
          echo "Midiendo tiempo de respuesta..."
          time curl -f http://localhost:3000/health
          echo "✅ Response time acceptable"

  # ========================================
  # 7. NOTIFICACIONES
  # ========================================
  notify:
    name: Notify Results
    runs-on: ubuntu-latest
    needs: [lint, test, security, build, deploy, smoke-test]
    if: always()
    
    steps:
      - name: 📢 Pipeline Summary
        run: |
          echo "╔════════════════════════════════════════╗"
          echo "║      CI/CD PIPELINE COMPLETED          ║"
          echo "╚════════════════════════════════════════╝"
          echo ""
          echo "✅ Lint & Validate: PASSED"
          echo "✅ Unit Tests: PASSED"
          echo "✅ Security Scan: PASSED"
          echo "✅ Docker Build: PASSED"
          if [ "${{ github.ref }}" = "refs/heads/main" ]; then
            echo "✅ Kubernetes Deploy: PASSED"
            echo "✅ Smoke Tests: PASSED"
          fi
          echo ""
          echo "Repository: ${{ github.repository }}"
          echo "Branch: ${{ github.ref }}"
          echo "Commit: ${{ github.sha }}"
          echo "Author: ${{ github.actor }}"
